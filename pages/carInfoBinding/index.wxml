<!--pages/carInfoBinding/index.wxml-->

<view class='container'>
 <view class='tab-wrapper'>
  <view id='tableft' class='tab-left {{currentTab === 0 ? "tab-active":""}}' bindtap='switchTab'>已绑车辆</view>
  <view id='tabright' class='tab-right {{currentTab === 1 ? "tab-active" : ""}}' bindtap='switchTab'>申请绑定</view>
 </view>
 <view class='content-wrapper' wx:if='{{currentTab === 0}}'><carList/></view>
 <view class='content-wrapper' wx:if='{{currentTab === 1}}'><carApply/></view>
</view>